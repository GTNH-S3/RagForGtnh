import {extractKeywords,testModel,queryTransformation} from '../ai/aiFunctions.ts';



 queryTransformation("Is the average temperature in summer in Berlin ore an its verage sıckalık in 2005").then(console.log);

